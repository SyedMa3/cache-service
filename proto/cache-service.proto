syntax = "proto3";
option go_package = "./";

package proto;

message SetRequest {
    string key = 1;
    bytes value = 2;
}

message GetRequest {
    string key = 1;
}

message Response {
    bytes value = 2;
}

service rpcService {
    rpc Get(GetRequest) returns (Response);
    rpc Set(SetRequest) returns (Response);
}